"""edge/speaker/ — Speaker tracking / voice consistency (final_use.md §4, Phase 4A).

Owner: edge/runtime agent. ECAPA-TDNN speaker embeddings for speaker identity and
style continuity, consistent with server/pipeline/speaker.py. Models load lazily.
"""

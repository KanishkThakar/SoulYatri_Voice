"""aux/tool_router/ — Tool contracts and routing (final_use.md §4, Phase 12C).

Owner: auxiliary reasoning agent. Tool schema, timeout/retry policy, safe-tool allowlist,
and text-brain escalation. Tool failures must degrade gracefully without breaking the
voice runtime.
"""

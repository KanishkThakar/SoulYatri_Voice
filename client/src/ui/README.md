# client/ui

Owner: client agent (final_use.md §4).

Visible session state and conversation UI. TypeScript/React components. The existing
`client/src/components/*` (VoiceInterface, TranscriptPanel, ConnectionStatus,
AudioVisualizer) are the current implementation.
